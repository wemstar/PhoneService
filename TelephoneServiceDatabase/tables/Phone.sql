CREATE TABLE [dbo].[Phone]
(
	[Phone_ID] INT NOT NULL PRIMARY KEY IDENTITY(1,1), 
    [Color] TEXT NOT NULL, 
    [State] NCHAR(40) NOT NULL, 
    [Model_Name] NCHAR(40) NOT NULL, 
    [Serial_Sumber] NCHAR(40) NOT NULL, 
    
)
